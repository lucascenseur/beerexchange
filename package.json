{
  "name": "beer-exchange",
  "version": "1.0.0",
  "description": "Site web pour soirées étudiantes avec gestion des bières en temps réel",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && PORT=3001 npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["beer", "exchange", "student", "party", "real-time"],
  "author": "Beer Exchange Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
